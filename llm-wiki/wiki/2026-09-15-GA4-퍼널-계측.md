---
확인 날짜: 2026-09-15
근거: GitHub Issue #125, 상위 이슈 #124, 노션 기능 정의서 `GA4 사용자 행동 계측`, raw/2026-09-15-GA4-퍼널-계측-설계-session-log.md, raw/2026-09-15-GA4-측정ID-배선과-실측-session-log.md
---

# GA4 퍼널 계측

## 범위

이슈 #125의 1차 범위는 퍼널 이벤트 12종이고 그중 **10종과 계측 기반**이 먼저 들어갔습니다.
이슈 #126이 인터뷰 안쪽 8종을 이어 붙였습니다(`interview_started` 포함). 남은 것은 후보 화면에
붙는 3종이고 이유는 `2026-09-15-GA4-계측-후속-backlog.md` 2번에 있습니다.

## 모듈 경계

| 파일 | 하는 일 |
| --- | --- |
| `src/lib/analytics/ga.ts` | 측정 ID 판정과 gtag 호출. 이벤트 어휘를 모릅니다 |
| `src/lib/analytics/user-id.ts` | `githubUserId`를 HMAC으로 바꿉니다. 서버 전용입니다 |
| `src/features/analytics/events.ts` | 이벤트 이름과 파라미터 타입, 공통 파라미터 세터, 버킷 |
| `src/features/analytics/analysis-events.ts` | 분석 상태 전이를 이벤트로 옮기는 순수 함수 |
| `src/features/analytics/ga-script.tsx` | gtag 스크립트 두 조각 |
| `src/features/analytics/analytics-session.tsx` | `user_id` 설정과 `login_result` |

어휘를 `lib/`이 아니라 `features/`에 둔 이유입니다. 파라미터 값을 `LoadingPhase`나 `EmptyKind` 같은
화면 도메인 타입에서 유도하는데 그 타입이 `features/`에 있습니다. `lib/`이 `features/`를 가져오면
의존 방향이 뒤집히므로 전송부는 문자열과 원시값만 받습니다.

## 이벤트와 붙는 자리

| 이벤트 | 붙는 자리 | 파라미터 |
| --- | --- | --- |
| login_view | `LoginScreen` 마운트 | auth_error |
| login_start | `LoginLink` 클릭 | 없음 |
| login_result | 콜백이 붙인 `?login=success` 또는 세션 없는 `?auth_error=` | success, error_kind |
| repo_list_loaded | `fetchRepositoriesFromApi` 성공 | repo_count, duration_ms |
| analysis_requested | `RepositoryFlow`의 분석 시작 | contribution_item_count |
| analysis_stage_done | `AnalysisState` 단계 전이 | stage, duration_ms |
| analysis_succeeded | `AnalysisState` success | candidate_count, commit_count_bucket, duration_ms |
| analysis_empty | `AnalysisState` empty | empty_kind |
| analysis_failed | `AnalysisState` error | error_kind, recovery, stage |
| analysis_retried | 오류 화면의 재시도 | error_kind, retry_scope |

`repo_visibility`와 `repo_language`는 `analysis_requested`의 파라미터로 따로 싣지 않습니다. 분석을
시작할 때 공통 파라미터로 세우므로 그 이벤트부터 자동으로 붙습니다.

### 공통 파라미터

`user_id`, `flow_id`, `entry_path`, `repo_visibility`, `repo_language`입니다. 우리 쪽에 맵을 따로 두지 않고 gtag의
`set`을 씁니다. gtag가 이미 같은 일을 하고, 맵을 두면 두 곳이 어긋날 수 있습니다. 스크립트가 아직
로드되지 않았어도 호출 순서가 `dataLayer`에 쌓이므로 뒤따르는 이벤트가 값을 놓치지 않습니다.

`flow_id`는 흐름을 시작하는 순간 발급하고 Repository 선택으로 돌아가면 비웁니다. 새 분석은 분석을
시작할 때, 이어가기는 저장된 인터뷰를 열 때입니다. 재시도는 같은 값을 그대로 씁니다. 실패한 분석과
그 재시도는 한 번의 분석 시도입니다.

## 인터뷰 안쪽 이벤트 (이슈 #126)

1차가 분석까지 덮고 2차가 인터뷰 안쪽을 잇습니다. 이벤트 8종이고 붙는 자리는 아래와 같습니다.

| 이벤트 | 붙는 자리 | 파라미터 |
| --- | --- | --- |
| interview_started | `InterviewScreen` 마운트 | turn |
| question_shown | `useInterviewStream`의 첫 조각 도착 | turn, block, element, ttft_ms |
| answer_submitted | `useInterviewStream`의 `submitAnswer` | turn, answer_length_bucket, think_time_ms |
| block_progressed | `useExperienceInterview`의 `onBeforeQuestion` | block, element, response, evaluation |
| interview_stream_failed | 질문 스트림의 `onError` | turn, error_kind |
| interview_completed | `useExperienceInterview`의 두 종료 경로 | end_reason, turn, filled_blocks |
| interview_abandoned | `RepositoryFlow`의 `navigate` | turn, filled_blocks |
| interview_leave_canceled | 이탈 확인 대화의 "Continue the interview" | turn, filled_blocks |

값은 전부 도메인 유니온에서 가져옵니다. `BlockKind`, `BlockElement`, `TargetResponse`,
`ProgressReason`, `InterviewStreamErrorKind`, `ExperienceInterviewEndReason`이 그대로 파라미터
타입입니다. 계측 전용 문자열을 새로 짓지 않는다는 결정을 1차와 같은 방식으로 지킵니다.

### 진입 경로를 공통 파라미터로 가릅니다

`entry_path`가 세 갈래를 가릅니다. `new_analysis`는 저장소를 골라 분석을 돌리는 갈래,
`stored_analysis`는 저장된 분석의 후보 목록을 다시 여는 갈래(이슈 #116), `resumed_interview`는
저장된 인터뷰의 대화를 잇는 갈래(이슈 #115)입니다.

뒤의 둘은 `analysis_requested`부터 `analysis_succeeded`까지를 하나도 거치지 않아서, 이 값이 없으면
리포트에서 인터뷰 시작 수가 분석 성공 수보다 커 보입니다. 그것을 모르고 보면 분석 단계에 문제가
있다고 읽게 됩니다.

`flow_id`의 수명을 저장 경로까지 늘렸습니다. 정의가 "분석 한 번"에서 "한 번의 흐름"으로 넓어졌고,
그래서 `startAnalysisFlow`와 `clearAnalysisFlow`를 `startFlow`와 `clearFlow`로 고쳤습니다. 발급은
`RepositoryFlow`의 `navigate` 한 곳에서 합니다. 모든 화면 이동이 그 함수를 지나므로 나중에 생긴
경로가 빠져나가지 않습니다.

분석 화면 안에서 `new_analysis`와 `stored_analysis`를 가르는 것은 `analysisId`입니다. 이 값이 있으면
저장된 분석을 여는 것이고(`openAnalysis`), 없으면 `startAnalysis`가 이미 흐름을 세운 뒤입니다.
`analysisId` 쪽을 세우지 않고 지나가면 직전 이어가기 흐름의 값이 그대로 남아, 저장된 분석에서 새로
시작한 인터뷰가 `resumed_interview`로 잡힙니다. develop 머지 뒤 실제로 그렇게 되어 있었습니다.

저장소 문맥을 세운 적이 있는지는 `flowApplied`와 따로 둡니다. 로그인하자마자 저장된 인터뷰를
이어가면 분석을 한 번도 하지 않은 채 흐름이 시작되는데, 두 값을 한 가드로 묶으면 세운 적 없는
`repo_visibility`를 null로 지우게 되고 gtag가 그 자리를 빈 문자열로 직렬화해 이후 모든 이벤트에
실어 보냅니다. `user_id`에서 실측한 것과 같은 동작입니다.

### 이탈을 다이얼로그가 아니라 이동에서 셉니다

이슈 #126 본문은 이탈 확인 다이얼로그의 두 선택지를 각각 이벤트로 남기면 된다고 적었습니다. 그
전제가 이미 바뀌어 있었습니다. #115와 #127 이후 `repository-flow.tsx`는 저장되지 않은 답변이 있을
때만 다이얼로그를 띄웁니다. 저장이 끝난 상태로 나가면 묻지 않고 그냥 나갑니다.

다이얼로그에만 붙이면 `interview_abandoned`가 "저장 안 된 답변을 버리고 나간 경우"만 세게 되어,
"중도 종료가 몇 번째 턴에 몰리는지"라는 Goal에 답하지 못합니다. 그래서 이탈은 `navigate` 안의 이동
경로에 붙이고, 다이얼로그의 두 선택지는 따로 셉니다.

끝낸 대화를 떠나는 것은 이탈이 아닙니다. 끝내기도 요약 화면으로 옮기느라 `navigate`를 한 번 더
지나므로, 가르지 않으면 끝까지 한 사용자가 중도 이탈로도 한 번 잡혀 두 수가 모두 틀립니다. 판정은
훅이 올려 준 `isEnded`를 그대로 씁니다. 확인 대화를 건너뛰었는지 같은 이동 방식으로 미루어 보지
않습니다. 끝내기 말고도 확인을 건너뛰는 경로가 있기 때문입니다.

### 진행 상황을 ref로 올려 보냅니다

이탈을 세는 자리(`navigate`)는 턴 수도 채운 블록 수도 모릅니다. `InterviewScreen`이
`onProgressChange`로 올려 보내고 `RepositoryFlow`가 ref에 담습니다. 상태로 두면 턴마다 흐름 전체가
다시 그려져, 스트리밍 중 리렌더 범위를 최소로 둔다는 이 서비스의 제약과 부딪힙니다. 이미 같은
이유로 ref인 `interviewActiveRef`·`hasUnsavedRef` 옆에 둡니다.

### 턴 번호를 스트림 훅이 다시 세지 않습니다

`useInterviewStream`이 `turnsUsed`를 옵션으로 받습니다. 완료 대기 안내에 대한 보충 답변은 턴으로
세지 않는데(설계 6-1절) 그 판정은 `useExperienceInterview`에 있습니다. 스트림 훅이 메시지를 다시
세면 두 곳이 갈라져 화면은 멀쩡한데 계측만 조용히 틀립니다.

### 두 시간의 기준점

`ttft_ms`는 요청을 보낸 시점부터 질문의 첫 조각까지입니다. 질문이 다 도착한 시점이 아니라 첫
조각으로 재는 이유는 사용자가 읽기 시작할 수 있는 시점이 거기이기 때문입니다.

`think_time_ms`는 그 첫 조각부터 제출까지입니다. 입력을 시작한 시점부터 재면 질문을 읽고 망설인
시간이 빠져, 질문이 답하기 어려운지를 판단할 수 없습니다. 첫 조각 시점을 모르는 경우(완료 대기
안내처럼 스트림 없이 질문 자리를 채운 경우)에는 싣지 않습니다. 0으로 채우면 즉답과 구분되지
않습니다.

두 값 모두 ref에 담습니다. 상태로 두면 조각이 올 때마다 다시 그리게 됩니다.

### 예외 격리를 한 겹 더 봅니다

새 호출 지점마다 인자 계산이 `trackEvent`의 try/catch 바깥에 있습니다. `filledBlockCount` 호출,
시각 계산, 버킷 변환이 모두 그렇습니다. 그래서 호출 지점마다 인자 계산까지 try 안에 넣었습니다.
여기서 던지면 종료가 멈추거나, 도착한 조각이 화면에 붙지 못하거나, 답변이 대화에 들어가지 못하고
사라집니다.

### 아직 없는 후보 화면 3종

`candidate_confirmed`, `candidate_detail_opened`, `evidence_expanded`입니다. 경위는
`2026-09-15-GA4-계측-후속-backlog.md` 2번에 있습니다.

## 화면과 계측이 같은 분류를 보게 한 세 가지

계측 전용 문자열을 새로 짓지 않는다는 기능 정의서의 결정을 지키려고 세 곳을 옮기거나 고정했습니다.

**단계 어휘를 화면에서 도메인으로 옮겼습니다.** `LoadingPhase`는 `step` 5종이고 `details` 하나가
`phase`로 `commit_details`·`repository_metadata` 둘로 갈립니다. 6단계로 펴는 매핑이 Loading 체크리스트
안에만 있어서, 그대로 두면 화면 개편이 `analysis_stage_done`의 단계 이름을 함께 바꿉니다.
`ANALYSIS_STAGES`와 `analysisStageOf`를 `repository-analysis.ts`에 두고 화면과 계측이 같은 함수를 봅니다.

**Empty 갈래에 이름을 붙였습니다.** `no_final_candidates`는 `reason`을 함께 실어야 해서 `AnalysisState`의
별도 변형이고 `EmptyKind`에 들어 있지 않습니다. 화면 안내표와 `empty_kind`가 모두 다섯 갈래를 다뤄야
하므로 `AnalysisEmptyKind`로 묶었습니다.

**`auth_error`는 화면 안내표를 기준으로 거릅니다.** 주소창의 쿼리는 아무 값이나 들어올 수 있습니다.
`AUTH_ERROR_COPY`에 없는 값은 `unknown`으로 묶어 GA4 디멘션에 임의 문자열이 쌓이는 것을 막습니다.

## `login_result`를 세션 유무로 판정하지 않는 이유

기능 정의서의 발생 시점은 "세션 확인 후 첫 렌더"였습니다. 이 앱에서는 그 조건이 로그인 직후에만
성립하지 않습니다. 세션 쿠키가 8시간을 살고, 새로고침마다 성립하며, 로그아웃이 `router.refresh()`로
페이지를 다시 실행시킵니다. 그대로 두면 `login_result`가 로그인 성공 수가 아니라 페이지 로드 수를
셉니다. "세션이 있다"는 "방금 로그인했다"의 대리 지표입니다.

대신 OAuth 콜백이 성공 리다이렉트에 `?login=success`를 붙입니다. 실패는 이미 `?auth_error=`로 오고
있어 성공과 실패가 같은 방식으로 갈립니다. 표시는 한 번 읽고 `router.replace("/")`로 지웁니다.
실패 표시는 지우지 않습니다. 로그인 화면이 그 값으로 오류 안내를 그립니다.

## 스크립트를 두 조각으로 나눈 이유

부트스트랩(`dataLayer`와 `gtag` 정의)은 평범한 인라인 `<script>`이고 라이브러리 본체만 `next/script`의
`afterInteractive`입니다.

부트스트랩까지 `afterInteractive`로 두면 hydration이 얼마간 진행된 뒤에야 주입되는데, 로그인 화면이
마운트되며 보내는 `login_view`가 그보다 먼저 일어나 퍼널의 첫 마디를 통째로 잃습니다. 인라인
`<script>`는 서버가 그린 HTML 안에서 파싱 순서대로 실행되므로 hydration 전에 `window.gtag`가
존재합니다. `beforeInteractive`는 Next.js 16.3.1 문서가 봇 탐지기나 쿠키 동의 관리자 같은 임계
스크립트로 한정하고 있어 쓰지 않았습니다.

측정 ID가 없으면 두 조각 모두 그리지 않습니다. 2026-09-15 실측으로 서버가 보낸 HTML에
`googletagmanager`도 `dataLayer`도 들어 있지 않은 것을 확인했습니다.

## 예외 격리

계측 실패가 서비스 오류로 보이면 안 된다는 제약을 세 겹으로 지킵니다.

- `lib/analytics/ga.ts`의 gtag 호출을 try/catch로 감쌉니다. 측정 ID가 없거나 `window.gtag`가 없으면
  그냥 반환합니다. 광고 차단기로 스크립트가 막히는 경우는 오류가 아니라 정상 경로입니다.
- `events.ts`의 `trackEvent`와 공통 파라미터 세터도 감쌉니다. 이 함수들은 데이터 조회 함수 안에서도
  불리고(`repository-client.ts`), 거기서 던지면 계측 실패가 조회 실패가 됩니다.
- 분석 화면의 `reportAnalysis`도 감쌉니다. 이 경로는 상태를 화면에 반영하기 직전에 실행되므로 여기서
  던지면 `setState`에 닿지 못해 화면이 로딩에 멈춥니다.

세 번째는 구현 중 테스트가 실제로 잡아낸 경로입니다. 경위는 세션 로그에 있습니다.

## `user_id` 변환과 세션 복호화

`page.tsx`가 세션 쿠키를 복호화해 `githubUserId`를 얻고 `GA_USER_ID_HMAC_SECRET`으로 HMAC-SHA256
16진 문자열(64자)을 만듭니다. 비밀값이 없으면 null이고 계측만 익명으로 떨어집니다.

복호화를 try/catch로 감쌉니다. `decryptGitHubSession`은 만료된 쿠키, 옛 형식의 쿠키, 암호화 키가 없는
서버에서 모두 던집니다. 이 페이지는 지금까지 쿠키의 존재만 보고 화면을 갈랐고, 만료된 쿠키를 든
사용자는 화면을 받은 뒤 API 호출에서 재로그인 안내를 받습니다. 계측 때문에 그 경로가 서버 오류로
바뀌면 안 됩니다.

값이 없을 때는 gtag를 아예 부르지 않습니다. `gtag('set', { user_id: null })`을 부르면 gtag가 그
자리를 빈 문자열로 직렬화해 **이후 모든 이벤트에** `uid=`를 실어 보냅니다. 2026-09-15에 수집 요청을
가로채 확인했고, "로그인 전에는 붙지 않습니다"라는 기능 정의서의 정의와 어긋납니다. 한 번이라도 세운
뒤의 null은 그대로 보냅니다. 로그아웃은 새로고침 없이 서버 컴포넌트만 다시 그리므로, 지우지 않으면
앞 사용자의 `user_id`가 계속 붙습니다.

## 환경변수

| 이름 | 위치 | 없을 때 |
| --- | --- | --- |
| `NEXT_PUBLIC_GA_MEASUREMENT_ID` | 빌드 시점 인라인, 브라우저가 읽습니다 | 스크립트를 싣지 않고 모든 호출이 아무 일도 하지 않습니다 |
| `GA_USER_ID_HMAC_SECRET` | 서버 전용 | `user_id` 없이 집계합니다 |

측정 ID는 인라인 부트스트랩 본문에 문자열로 박히므로 영숫자와 하이픈만 통과시킵니다. 형식을 벗어난
값은 설정이 잘못된 것이므로 값이 없는 것과 같이 다룹니다.

## 서버와 클라이언트 경계

`page.tsx`는 서버 컴포넌트입니다. 여기서 부르는 판정 함수는 `"use client"`가 붙은 파일에 두면 안
됩니다. Next.js가 클라이언트 모듈의 함수를 서버에서 부르는 것을 렌더 단계에서 500으로 끊습니다.

`AUTH_ERROR_COPY`와 `toAuthErrorParam`을 `src/features/auth/auth-error.ts`에 둔 이유입니다. 화면과
계측이 같은 표를 보되 어느 쪽도 경계를 넘지 않습니다.

**이 결함은 테스트로 잡히지 않습니다.** vitest는 모듈 그래프가 하나라 `"use client"` 지시어가 아무
경계도 만들지 않고, `next build`도 통과합니다. 서버를 띄우고 그 주소를 실제로 요청해야 드러납니다.
2026-09-15에 이 경로로 잡았습니다.

## 확인 절차

### 로그인 경계 네 건은 스크립트가 확인합니다

```
# .env.local에 NEXT_PUBLIC_GA_MEASUREMENT_ID를 두고
npm run build
npx next start -p 3125
npx tsx scripts/measure-ga-events.mts --url http://127.0.0.1:3125
```

`login_view`, `login_start`, `login_result`, 그리고 오류 화면의 `login_view`를 확인하고 기대와 다르면
종료 코드 1입니다. 기본은 가로채기라 확인용 히트가 실제 속성에 쌓이지 않습니다. `--send`를 주면
통과시킵니다.

이벤트와 파라미터를 수집 요청이 아니라 `window.dataLayer`에서 읽습니다. gtag가 클릭 직후의 이벤트를
`navigator.sendBeacon`으로 보내는데 Playwright가 그 본문을 읽지 못합니다. 전송이 실제로 일어나는지는
수집 요청 수로 따로 봅니다.

2026-09-15 실행 결과입니다.

```
수집 요청 4건
  event login_view {}
  event login_start {}
  event login_result {"success":false,"error_kind":"config_missing"}
  event login_view {"auth_error":"config_missing"}
```

### 나머지 여섯 종은 DebugView로 확인합니다

GitHub OAuth와 LLM 호출이 있어야 도달하므로 자동으로 확인할 수 없습니다.

1. 크롬에 Google Analytics Debugger 확장을 설치해 켭니다. 코드에 `debug_mode`를 넣지 않는 이유는
   그 값이 프로덕션 트래픽까지 디버그로 표시하기 때문입니다.
2. `.env.local`에 측정 ID와 GitHub OAuth, LLM 환경변수를 채우고 `npm run build`, `npx next start`를
   실행합니다.
3. GA4 관리 화면의 **관리 > 데이터 표시 > DebugView**를 엽니다.
4. 로그인하고 저장소를 골라 분석을 끝까지 돌립니다. `repo_list_loaded`, `analysis_requested`,
   `analysis_stage_done` 여섯 건, 그리고 `analysis_succeeded`나 `analysis_empty`나 `analysis_failed`
   가운데 하나가 도착하는지 봅니다. 실패했다면 재시도를 눌러 `analysis_retried`도 봅니다.
5. 각 이벤트를 눌러 파라미터를 펴고 금지 값(저장소 owner와 이름, 로그인 아이디, 커밋 SHA, 파일 경로)이
   없는지, `user_id`가 64자 16진 문자열인지 확인합니다.

## GA4 속성 설정

2026-09-15에 확인한 값입니다. 측정 ID는 페이지 소스에 그대로 실리는 공개 값이라 여기 남깁니다.

| 항목 | 값 | 상태 |
| --- | --- | --- |
| 측정 ID | `G-FZYPE79375` | 확인 |
| 보고 시간대 | 대한민국 표준시 | 확인 |
| 통화 | KRW | 확인 |
| 이벤트 데이터 보관 | 14개월 | 확인 |
| Google 신호 데이터 | 꺼짐 | 확인 |
| 맞춤 측정기준 11개, 측정항목 4개 | 등록 완료 | 확인 |

시간대와 통화는 속성을 만든 뒤에 바꿔도 과거 데이터에 소급되지 않습니다. 데이터 보관은 표준 속성
기본값이 2개월이라 명시적으로 14개월로 바꿔야 합니다.

Google 신호 데이터는 속성을 만들 때 켜져 있던 것을 2026-09-15에 껐습니다. 이 계측이 동의 배너를
만들지 않기로 한 근거가 "광고 목적 수집을 하지 않고 Google 신호 데이터를 끄며 개인을 지목할 수 있는
값을 모두 금지하므로 남는 것은 화면 전환 기록뿐"이라는 것입니다. 켜져 있으면 구글 계정 로그인
사용자의 광고 개인화 식별자가 붙어 그 전제가 성립하지 않습니다. 끈 시점까지 이 속성에 들어간 것은
확인용 히트 4건뿐입니다.

### 등록한 맞춤 정의

1차에 등록한 디멘션(이벤트 범위)은 `flow_id`, `auth_error`, `success`, `error_kind`, `empty_kind`,
`recovery`, `stage`, `retry_scope`, `repo_visibility`, `repo_language`, `commit_count_bucket`
11개입니다. 측정항목은 `duration_ms`(밀리초), `repo_count`, `contribution_item_count`,
`candidate_count` 4개입니다. 수치를 디멘션으로 등록하면 문자열로 취급돼 평균과 합계가 나오지
않습니다.

이슈 #126이 디멘션 9개와 측정항목 2개를 더합니다. 디멘션은 `entry_path`, `end_reason`, `block`,
`element`, `response`, `evaluation`, `answer_length_bucket`, `turn`, `filled_blocks`이고 측정항목은
`ttft_ms`, `think_time_ms`입니다. 합쳐서 디멘션 20개, 측정항목 6개이고 한도는 각각 50개입니다.
`error_kind`는 스트림 실패가 그대로 다시 씁니다.

`turn`과 `filled_blocks`를 수치인데도 디멘션으로 등록합니다. 이 둘로 알고 싶은 것이 평균이 아니라
분포이기 때문입니다. 중도 종료가 몇 번째 턴에 몰리는지는 턴별로 묶어야 보이고, 평균 턴 수는 그
질문에 답하지 않습니다.

등록은 소급되지 않고 리포트 반영까지 하루에서 이틀 걸립니다. DebugView와 실시간은 등록과 무관하게
파라미터를 바로 보여주므로 이벤트가 제대로 나가는지는 등록을 기다리지 않고 확인할 수 있습니다.

`verifiability`와 `evidence_type`은 아직 없습니다. 후보 화면 3종과 함께 미뤘습니다.

## 확인 필요

- 로그인 뒤 DebugView 확인이 남았습니다. 1차 여섯 종에 더해 이슈 #126의 8종도 같은 절차로 봅니다.
  인터뷰를 한 번 끝까지 돌리고, 도중에 나가는 경로와 확인 대화에서 돌아오는 경로도 한 번씩 밟습니다.
- GA4 관리 화면에 디멘션 9개와 측정항목 2개를 등록해야 합니다. 등록은 소급되지 않습니다.
- 프로덕션(Vercel)에 `NEXT_PUBLIC_GA_MEASUREMENT_ID`와 `GA_USER_ID_HMAC_SECRET`을 넣어야 합니다.
  측정 ID는 빌드 시점에 인라인되므로 값을 넣은 뒤 다시 빌드해야 합니다. HMAC 비밀값은 프로덕션용으로
  따로 만들고, 값이 바뀌면 같은 사용자가 다른 사람으로 집계되므로 유출 사고가 아닌 한 교체하지 않습니다.
- GA4 향상된 측정이 `page_view`와 `scroll` 같은 자동 이벤트를 함께 보냅니다. 이 계측이 만든 것이
  아니고 퍼널 해석에 쓰지 않습니다.
- 파라미터 어휘가 26개입니다. 기능 정의서는 1차 기준으로 "15개 안쪽"이라고 적었습니다. 한도 50에는
  여유가 있지만 기능 정의서의 수를 고쳐야 합니다.
