---
확인 날짜: 2026-09-11
근거: PR #104 Codex 1차 리뷰, llm-wiki/wiki/2026-08-20-코드리뷰-전략.md
---

# PAAR 경험 블록 후속 backlog

> 2026-09-11 구현 검토 보완: 아래 2번의 P2 판정과 “운영 경로도 자동 재시도” 설명은 현재 코드에 맞지 않습니다. 실패한 답변의 처리 상태가 다음 턴 성공 시 지워지는 것을 재현해 정보 누락을 P1로 재판정했습니다. 운영 route는 리듀서 거절 뒤 즉시 502를 반환합니다. 근거는 `../output/2026-09-11-PAAR-경험블록-구현검토.md` 2·8절과 `../raw/2026-09-11-PAAR-구현검토-session-log.md`입니다. 이전 판단 경위는 아래에 보존합니다.

> 2026-09-11 P1 반영 완료: 검토가 지적한 P1 다섯 묶음을 모두 반영했습니다(재질문 카운팅, 턴별 미반영
> 상태, 겹친 응답 적용 경계·언마운트 취소, 완료 대기 상태 보충 답변, 실패·충돌·응답 분류의 질문 생성
> 전달). 반영하며 재질문의 "다른 단서" 조건 미구현과 `lastOutcome`의 실모델 미실측이 새로 남았고
> 설계 개정 문서 12절에 적었습니다. 경위는 `../raw/2026-09-11-PAAR-구현검토-반영-session-log.md`입니다.
> 아래 1번 항목(원래 이슈 #89 P2)도 이번에 함께 해결됐습니다.

> 2026-09-11 P2 반영: 아래 세 묶음 중 1번(예약 정책)과 3번(실측-운영 대응)을 반영했고, 2번(판정
> 기준)은 실측으로 재확인한 결과 절반만 코드로 고쳤습니다. 경위는
> `../raw/2026-09-11-PAAR-구현검토-P2-재검증-session-log.md`입니다.

> 2026-09-11 독립 재검증: P1 다섯 묶음 중 P1-4("완료 대기 상태 보충 답변")가 불완전했습니다.
> `canSubmitAnswer` 조건만 풀고 `onBeforeQuestion`의 질문·답변 추출 로직과 서버의 이력 교대
> 검증을 그대로 둬, 보충 답변이 실제 새 질문을 열면 서버가 이력 모양을 거절하는 회귀가 있었습니다.
> 완료 대기 안내를 "질문" 자리에 직접 넣는 방식으로 다시 고쳤습니다. 나머지 P1 네 건과 P2 코드
> 수정 두 건은 재검증에서 결함이 나오지 않았습니다. 경위는
> `../raw/2026-09-11-PAAR-구현검토-재검증-결과-session-log.md`입니다.

## 2026-09-12 현재 상태 정정

아래의 P1 반영 완료 기록은 일반 경로에 대한 결과입니다. 추가 재검증에서는 P1 세 묶음이 남아 있습니다. 현재 답변 처리와 재처리가 겹치면 중단된 답변이 미반영 목록에서 빠지고, 재처리 큐는 unmount 뒤 남은 요청을 시작합니다. 완료 안내에 대한 보충 답변은 질문 예산을 차감합니다. unknown 뒤 재질문 평가가 실패하면 소진된 요소를 다시 선택합니다.

따라서 아래 3번의 “상태 훼손이나 사용자에게 보이는 오동작이 없다”는 판단을 전체 흐름에 적용할 수 없습니다. 현재 턴과 겹치는 재처리는 P1 수정 대상으로 함께 처리해야 합니다. 별도 P2/P3 후속으로만 남기지 않습니다. 근거는 [재검증 보고서](../output/2026-09-12-PAAR-수정사항-재검증.md)이며 경위는 [세션 로그](../raw/2026-09-12-PAAR-수정사항-재검증-session-log.md)에 있습니다.

> **2026-09-14 반영 완료.** 위 P1 세 묶음을 모두 고쳤습니다. 첫 번째와 두 번째(재처리·현재 답변의
> 경합, 언마운트 뒤 큐 잔여 실행)는 개별 patch가 아니라 `applyQueueRef`로 모든 블록 갱신 호출을
> 직렬 큐에 태우는 방식으로 근본 원인 자체를 없앴습니다. 동시 실행이 없으면 한 호출이 다른 호출을
> abort할 일도, 언마운트 뒤 큐가 다음 항목을 실행할 일도 없습니다. 세 번째(완료 안내 보충 답변의
> 턴 차감)는 직전 "질문"이 `READY_TO_FINISH_PROMPT`와 같은지 비교해 보충 답변을 가려내고 그때는
> `turnsUsed`를 올리지 않도록 고쳤습니다. 재검증 보고서가 "이번에 새로 발견한 결함은 아니다"라고
> 구분한 네 번째(재질문 뒤 평가 실패 시 소진된 요소 재선택, 원래 P1-1의 미해결 예외 경로)도 함께
> 고쳤습니다 — `progress.ts`의 `reaskUsed` 저장 플래그를 없애고 `isElementClosed`가 `askedCount`와
> `firstUnknownAskedCount`만으로 매번 다시 계산하게 해, 재질문의 응답을 받았는지와 무관하게 예산
> 소진을 판정합니다. 이 셋 모두 재검증 세션이 남긴 재현 테스트 원문(`../output/2026-09-12-PAAR-재검증-재현.test.tsx.txt`)의
> 시나리오를 참고해 새로 작성한 회귀 테스트로 확인했습니다. 테스트 1121→1122개, lint·typecheck
> 통과. 경위는 `../raw/2026-09-14-PAAR-추가재검증-P1-세묶음-반영-session-log.md`입니다.
>
> 아래 3번 항목("겹친 재시도가 중복 네트워크 호출을 낼 수 있습니다")이 언급하는 `callSeq` 가드는
> 이번에 제거됐습니다. 직렬 큐로 바뀌어도 그 항목이 지적한 낭비 호출 자체는 남아 있어(같은 턴이
> 큐에 두 번 들어가면 첫 번째가 이미 반영을 끝낸 뒤에도 두 번째가 그대로 실행됩니다), 항목 본문을
> 갱신했습니다.

## 구현 검토에서 분리한 후속 작업

2026-09-11 검토의 P1 다섯 묶음은 2026-09-11에 모두 반영을 마쳤습니다(커밋 `79de4ff`·`9b8809e`·`899c92f`·`4c1c107`·`baf49c1`, 경위는 `../raw/2026-09-11-PAAR-구현검토-반영-session-log.md`). 아래 P2 세 묶음의 반영 상태는 다음과 같습니다.

- **반영 완료(커밋 `e2e1b28`).** 미확인 블록 예약이 정보 확보 여부가 아니라 질문 방문 여부를 기준으로 하던 것을 고쳤습니다. `selectNextTarget`의 미확인 블록 판정에 `evaluation[block] === null` 조건을 더해, 다른 답변에서 이미 평가받은 블록은 질문을 보낸 적이 없어도 예약 대상에서 뺍니다. R8을 회귀 테스트로 옮겼습니다.
- **절반 반영.** 충분성과 targetResponse의 판정 기준을 2026-09-11 재실측(46건)으로 다시 확인했습니다. "merged 23턴에 sufficient=true가 없다"는 원래 지적은 재현되지 않았습니다(재실측에서 7건 발생, 같은 시나리오·프롬프트인데도 갈린 것으로 보아 샘플링 변동으로 판단). 반면 두 가지는 두 차례 실측 모두에서 재현됐습니다. (1) 관찰과 확인 방법이 정성적으로 다 있는 답변도 수치가 없다는 이유만으로 `not_done`으로 닫히는 문제는 **고쳤습니다**(커밋 `92f7241`, `block-prompt.ts` 평가 규칙에 "결과 블록은 수치를 요구하지 않는다"는 문장 추가, `recall` 시나리오 재실측으로 `sufficient=true`로 바뀌는 것을 확인). (2) targetElement가 겨냥하지 않은 블록 정보만 담은 답변을 `provided`로 잘못 분류하는 문제(`problem-chain-t5`)는 **고치지 않았습니다** — 반응 규칙 문구 자체가 이미 이 경우를 `unanswered`로 명시하고 있어 문구를 더 강화한다고 해결된다는 근거가 없습니다. 사람 정답을 더 붙여 검증해야 합니다.
- **반영 완료(커밋 `0ded168`·`c5a9659`).** 실측 집계와 운영 경로를 맞췄습니다. 측정 하네스가 다음 질문 호출에 `focus`(다음 시나리오 턴의 targetBlock·targetElement)와 `lastOutcome`을 싣도록 고쳐 운영 경로와 같은 모양의 입력을 재게 했습니다. 블록 갱신 비용과 질문 생성 비용을 `blockSpent`·`questionSpent`로 나눠 집계하도록 했습니다(46건 재실측: 블록 $0.02689, 질문 $0.06933, 합계 $0.09622). 다만 `selectNextTarget`으로 다음 대상을 실제로 정하고 그 생성된 질문을 다음 턴의 질문으로 그대로 쓰는 완전한 운영 경로 재현(합성 질문 완전 배제)은 아직 하지 않았습니다.

## 문서 목적

이슈 #89 PR(#104) Codex 1차 리뷰를 반영하면서 P2로 재판정해 후속으로 분리한 항목과, 이슈 #90 실측
과정에서 나온 후속 항목을 함께 기록합니다. 별도 GitHub Issue는 만들지 않습니다. 반영 경위는 각 항목
아래에 근거로 남긴 세션 로그에 있습니다.

## 미뤄 둔 항목

### 1. 리듀서가 targetBlock·영향받은 블록의 평가 누락을 거절하지 않습니다

`applyBlockUpdate`의 평가 검증 루프(`reducer.ts`)는 모델이 낸 `evaluation` 항목만 확인하고, 요청이
겨냥한 `targetBlock`이나 이번에 주장이 바뀐 블록의 평가가 빠졌는지는 확인하지 않습니다. 모델이
`evaluation: []`을 내도 통과하고, 그러면 그 블록의 이전 평가값이 낡은 채로 남습니다.

표시 문장 쪽에는 대응하는 가드가 있습니다("표시가 있던 블록의 주장이 바뀌면 새 표시가 반드시
와야 한다", 설계 문서 3-3절). 평가 쪽만 이 가드가 없는 비대칭이라 실제로 빠뜨린 것으로 보입니다.

**고칠 자리**는 `applyBlockUpdate`의 `context` 계약에 `targetBlock`을 추가하고, 평가 검증 루프에서
`targetBlock`과 `affected` 블록 각각의 평가가 응답에 포함됐는지 확인해 빠지면 `invalid_evaluation`으로
거절하는 것입니다.

**P2로 후속 분리한 이유**: 지금 이 값을 읽는 소비자가 없습니다. 이슈 #90(턴 진행과 블록 전환)의
"다음 질문 선택" 로직이 아직 구현되지 않아, 평가가 비거나 낡아도 관측 가능한 오동작이 당장 없습니다.
반면 `context` 계약 변경은 `reducer.test.ts`의 기존 호출부(18개)와 측정 하네스
(`block-update.measure.mts`) 3곳, `src/app/api/interview/experience-block/route.ts`를 함께 고쳐야 해서
"수정 비용이 매우 작은" 예외에 해당하지 않는다고 판단했습니다.

**#90 착수 시 함께 정합니다.** 그 이슈가 어차피 `applyBlockUpdate` 호출부(훅)를 새로 만들며 이
계약을 정해야 하므로, 지금 서버 쪽만 먼저 고치면 #90에서 또 손대야 합니다. 계약 변경 시 함께 고칠
문서는 `llm-wiki/wiki/2026-09-11-PAAR-경험블록-설계-개정.md` 3-3절("리듀서가 보장하는 것" 목록에
항목 추가)입니다.

**해결됨(2026-09-11, 이슈 #90 실측).** `context.targetBlock`을 추가하고 `targetBlock`·영향받은
블록의 평가 누락을 `invalid_evaluation`으로 거절하는 가드가 `reducer.ts`에 들어갔습니다. 실측
46건 중 2건에서 실제로 이 가드가 작동해 누락된 응답을 거절하는 것을 확인했습니다
(`raw/2026-09-11-PAAR-경험블록-턴진행-실측-session-log.md` 4절). 다만 그 거절이 아래 2번 항목으로
이어집니다.

> **2026-09-14(이슈 #91).** 블록 패널은 `evaluation`을 카드 상태 판정에 쓰지 않습니다. 문장 유무와
> 종료 여부만 봅니다. 이 항목이 고쳐지지 않아도 화면이 오표시되지는 않습니다. 항목 자체는 남습니다.

### 2. 리듀서 거절이 재시도로 회복되지 않으면 targetBlock 밖 정보가 그대로 버려집니다

운영 경로(측정 하네스도 동일)는 리듀서가 거절하면 같은 프롬프트로 한 번만 재시도합니다. 1번 항목의
가드가 targetBlock이 아닌 다른 블록(모델이 `ops`로 주장을 바꿨지만 `evaluation`을 빠뜨린 블록)의
누락을 잡아내는 건 맞지만, 모델이 같은 실수를 재시도에서도 반복하면(2026-09-11 실측 46건 중 2건,
merged-problem-chain-t1과 split-multi-block-t3) 그 턴 전체가 미반영으로 남아 targetBlock 밖 정보가
사라집니다. "질문이 문제만 물었더라도 사용자가 말한 대안·구현·결과를 버리면 다시 얻을 수 없다"는
`block-prompt.ts`의 규칙과 정면으로 부딪히는 손실입니다.

**고칠 자리 후보**는 재시도 프롬프트에 직전 거절 사유(어느 블록의 평가가 빠졌는지)를 덧붙이는 것과,
재시도 없이도 누락된 블록의 평가만 이전 값으로 채워 부분 반영하는 것 두 가지입니다. 후자는 "새 정보가
없으면 targetBlock 평가를 이전 값과 같게 낸다"는 기존 규칙을 targetBlock 밖 블록에도 넓히는
방향입니다.

**P2로 후속 분리한 이유**: 표본 46건 중 2건(4.3%)으로 재현율이 낮고, 이 값이 실제 인터뷰 진행에
얼마나 영향을 주는지(사용자가 targetBlock 밖 정보를 답변에 섞는 빈도) 아직 실사용 데이터가 없습니다.
`selectNextTarget`의 재질문 예산 판단은 이 정보 손실과 무관하게(다음 질문 대상은 이미 반영된 상태
기준으로 고르므로) 동작해 당장 관측 가능한 오동작은 없습니다.

### 3. 겹친 재시도가 중복 네트워크 호출을 낼 수 있습니다

`retryUnreflectedBlockUpdate()`를 짧은 간격으로 두 번 부르면(예: 사용자가 재시도 버튼을 빠르게
두 번 누름), `retryAllUnreflected`의 두 실행이 같은 미반영 턴을 각자 `applyTurn`으로 다시 호출할
수 있습니다. 2026-09-14에 모든 블록 갱신 호출을 직렬 큐(`applyQueueRef`)에 태우도록 바꿔 두 호출이
동시에 실행되는 일은 없어졌지만, 같은 턴이 큐에 두 번 들어가는 것 자체는 막지 않습니다. 첫 번째
호출이 성공해 그 턴을 미반영에서 지운 뒤에도, 이미 큐에 들어간 두 번째 호출은 그대로 실행됩니다.
동시 실행이 없어 응답 순서가 요청 순서와 어긋날 일도 없으므로 데이터 훼손은 여전히 없지만, 낭비
호출 자체는 막지 않습니다(2026-09-11 독립 재검증에서 발견, P3).

**P3로 남기는 이유**: 상태 훼손이나 사용자에게 보이는 오동작이 없고, 재시도 버튼은 화면(하위 이슈
D)이 아직 없어 실제로 짧은 간격 연타가 가능한지도 확인되지 않았습니다. 화면을 붙일 때 버튼 자체를
잠그는 것으로 충분히 막힐 가능성이 있습니다.

> **2026-09-14 부분 반영(이슈 #91).** 재시도 버튼이 생겼고 `isBlockUpdating`이 참인 동안 잠급니다.
> 연타로 큐에 여러 번 넣는 가장 흔한 경로는 막혔습니다. 다만 큐에 이미 들어간 중복은 그대로
> 실행되므로 항목 자체는 P3로 남깁니다. 같은 턴이 큐에 있으면 넣지 않는 가드가 근본 해결입니다.

> **2026-09-15 해결(PR #121 리뷰 2라운드).** 근본 해결을 넣었습니다. `applyTurn`이 큐에 들어가
> 있거나 실행 중인 턴을 `queuedTurnsRef`로 추적하고, 이미 등록된 턴은 다시 넣지 않습니다.
>
> 버튼 잠금으로 충분하다는 2026-09-14의 기대는 틀렸습니다. `isBlockUpdating`은 큐에 넣는 시점이
> 아니라 `runApplyTurn`이 자기 차례를 잡았을 때 참이 되므로, 잠기기 전에 두 번 들어갈 수 있습니다.
> 더 중요한 것은 **종료 경로**입니다. `endInterview`도 `retryAllUnreflected`를 부르는데 종료 버튼은
> `isBlockUpdating`으로 잠기지 않습니다. 재처리가 큐에 들어간 직후 종료하면 같은 턴이 두 번
> 등록됐습니다. 리뷰는 연타만 지적했고 이 경로는 반영 과정에서 찾았습니다.
>
> 추적 값을 `Set<string>`이 아니라 `Map<string, symbol>`로 둡니다. 종료는 진행 중이던 호출을 끊고
> 그 턴을 곧바로 다시 등록하는데(구현검토 P1-2, R6), 끊긴 호출이 나중에 정리될 때 자기가 넣은
> 항목이 아니라 새로 등록된 항목을 지우면 중복 판정에 구멍이 생깁니다. 자기 표식일 때만 지웁니다.
>
> 처음 넣은 가드는 범위가 너무 넓어 그 "끊고 한 번 더 반영"까지 중복으로 걸렀습니다. 기존 테스트가
> 잡았고, `endInterview`가 끊은 턴을 추적에서 빼는 것으로 갈랐습니다. 회귀 테스트 3건을 넣었습니다.

### 4. 열 턴이 네 블록을 채우기에 충분한지 확인하지 않았습니다

상위 이슈 #87을 닫으면서 분리한 항목입니다(2026-09-15). #87은 PAAR 네 블록을 채우는 인터뷰를
정의하면서 턴 상한을 열로 정했는데, **그 열이 실제로 네 블록을 채우기에 충분한지는 재지 않았습니다.**
`INTERVIEW_MAX_TURNS`가 열이고, 상한에 닿으면 완료 안내 없이 그대로 종료합니다(설계 3절 Approach 3).

지금 확인할 수 없는 이유는 표본이 없기 때문입니다. 실제 인터뷰를 여러 건 끝까지 진행해 종료 시점의
블록 채움 수를 모아야 하는데, 저장 계층이 없어 그 기록이 남지 않습니다(#115·#116의 범위). 화면은
`PAAR n/4`로 채움 수를 이미 보여 주므로 사람이 눈으로 세는 것은 가능하지만, 한두 건으로 상한을
조정하면 표본이 부족한 채로 숫자만 바뀝니다.

**판단 기준**: 열 턴을 다 쓰고도 블록이 둘 이하로 남는 경우가 반복되면 상한이 모자란 것입니다.
반대로 대여섯 턴에서 네 블록이 차고 남은 턴이 같은 내용을 되묻는 데 쓰이면 상한이 과합니다. 어느
쪽인지는 종료 시점의 `turnsUsed`와 채워진 블록 수를 함께 봐야 갈립니다.

**P3로 남기는 이유**: 상한이 맞지 않아도 사용자는 언제든 종료할 수 있고(#78), 블록이 덜 찬 채로
끝나도 종료 후 편집으로 직접 채울 수 있습니다(#91). 데이터 훼손이나 막힘이 없습니다. 저장 계층이
생겨 표본을 모을 수 있을 때 다시 봅니다.
