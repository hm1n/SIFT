# Sentry 브라우저 계측(이슈 #81)
#
# Sentry 프로젝트의 클라이언트 DSN입니다. 이 값이 없으면 `Sentry.init`을 부르지 않으므로 로컬
# 개발과 테스트에서 이벤트가 나가지 않습니다. 값을 넣으면 Core Web Vitals와 클라이언트 런타임
# 오류가 Sentry로 갑니다.
#
# `NEXT_PUBLIC_` 접두사가 붙은 값은 Next.js가 빌드 시점에 클라이언트 번들로 인라인합니다.
# 런타임 환경변수로 주입해도 브라우저가 읽지 못하므로 빌드 파이프라인에 있어야 합니다.
NEXT_PUBLIC_SENTRY_DSN=

# Sentry 서버 계측(이슈 #136)
#
# 서버와 엣지 런타임이 읽는 DSN입니다. 비어 있으면 위의 `NEXT_PUBLIC_SENTRY_DSN`으로 내려가고, 그것도
# 비어 있으면 서버에서 `Sentry.init`을 부르지 않습니다. 로컬 개발과 테스트에서 이벤트가 나가지 않는
# 상태가 둘 다 비어 있는 경우입니다.
#
# 이름을 따로 두는 이유는 나중에 서버 이벤트를 다른 Sentry 프로젝트로 나눌 때 배포 설정만 바꾸면
# 되게 하려는 것입니다. 지금은 같은 프로젝트를 가리키므로 하나만 채워도 됩니다.
#
# `NEXT_PUBLIC_` 접두사를 붙이지 않습니다. 읽는 쪽이 서버라 런타임 주입으로 동작하고, 빌드 파이프라인에
# 넣을 필요가 없습니다.
SENTRY_DSN=

# Sentry Source Map 업로드와 release 연결(이슈 #144)
#
# 빌드가 소스맵을 Sentry로 올리고 Release를 만들 때 쓰는 인증 토큰입니다. Organization Auth Token
# (`sntrys_`로 시작)을 권장합니다. 개인 토큰을 쓸 때 필요한 권한은 `project:releases`와 `org:read`입니다.
#
# 서버 전용 비밀값이고 빌드 시점에 필요합니다. `NEXT_PUBLIC_` 접두사를 붙이면 안 됩니다. 클라이언트
# 번들에 실려 누구나 이 프로젝트의 소스맵을 올리고 Release를 조작할 수 있게 됩니다.
#
# 비어 있어도 빌드는 끝납니다. SDK가 업로드와 Release 생성을 건너뛰고 경고만 남깁니다. 대신 그 빌드로
# 올라간 배포는 스택이 번들 기준이고 이벤트에 release가 붙지 않습니다. 로컬 빌드의 정상 상태입니다.
#
# org와 project 슬러그는 비밀값이 아니라 `next.config.ts`에 적혀 있습니다. 여기에 넣지 않습니다.
SENTRY_AUTH_TOKEN=

# 이 파일에는 아직 GitHub OAuth와 LLM 관련 환경변수가 없습니다. 이슈 #81 범위가 아니어서
# `llm-wiki/wiki/2026-09-08-sentry-계측-후속-backlog.md` 6번으로 남겼습니다.

# GA4 퍼널 계측(이슈 #125)
#
# GA4 웹 데이터 스트림의 측정 ID입니다. `G-` 뒤에 영숫자인 형식만 받고 그 밖의 값은 설정이 잘못된
# 것이므로 없는 것과 같이 다룹니다. 이 값이 없거나 공백이면 gtag 스크립트를
# 아예 싣지 않으므로 네트워크 요청이 일어나지 않고 모든 이벤트 호출이 아무 일도 하지 않습니다.
# 로컬 개발과 테스트의 정상 상태이고 콘솔 경고도 남기지 않습니다.
#
# `NEXT_PUBLIC_` 접두사가 붙은 값은 Next.js가 빌드 시점에 클라이언트 번들로 인라인합니다. 읽는 쪽이
# 브라우저이므로 런타임 주입으로는 동작하지 않고 빌드 파이프라인에 있어야 합니다. 측정 ID는 페이지
# 소스에 그대로 실리는 공개 값이라 번들에 들어가도 무해합니다.
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# GA4 `user_id`를 만드는 HMAC 비밀값입니다. 서버 전용입니다.
#
# GitHub 사용자 번호를 그대로 보내면 GitHub 공개 API로 계정 아이디를 역추적할 수 있어, 금지한 로그인
# 아이디를 함께 보내는 것이 됩니다. 이 값을 섞어 되돌릴 수 없는 해시로 바꿔 보냅니다.
#
# `NEXT_PUBLIC_` 접두사를 붙이면 안 됩니다. 클라이언트 번들에 실려 누구나 같은 해시를 계산할 수 있고
# 변환의 의미가 사라집니다. 값이 바뀌면 같은 사용자가 다른 사람으로 집계되므로 유출 사고가 아닌 한
# 교체하지 않습니다. 비어 있으면 `user_id` 없이 집계합니다.
GA_USER_ID_HMAC_SECRET=

# Neon Postgres 접속(이슈 #114)
#
# Vercel 마켓플레이스에서 Neon을 연결하거나 `neon link`를 실행하면 이 두 값이 주입됩니다. 로컬에서는
# `neon link`가 `.env.local`에 써 주므로 이 파일의 값을 손으로 채울 일은 없습니다.
#
# DATABASE_URL은 연결 풀을 거치는 주소이고 애플리케이션 질의가 씁니다. DATABASE_URL_UNPOOLED는
# 풀을 거치지 않는 주소이고 `npm run db:schema`가 쓰는 DDL 전용입니다. 없으면 DATABASE_URL로
# 내려갑니다.
#
# 표 두 개를 만들려면 값을 채운 뒤 `npm run db:schema`를 실행합니다. 모든 문장이 `if not exists`라
# 여러 번 실행해도 결과가 같습니다.
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# 90일 자동 정리 cron 인증(이슈 #116)
#
# Vercel 프로젝트에 이 환경변수를 두면 Cron이 `/api/cron/purge`를 부를 때 `Authorization: Bearer <값>`
# 헤더를 함께 보냅니다. 경로는 그 헤더로만 열리고, 값이 없으면 아무도 열 수 없습니다(401). 16자 이상의
# 무작위 문자열을 권장합니다.
#
# 로컬에서는 cron이 돌지 않습니다(`next dev`는 cron을 실행하지 않습니다). 손으로 확인하려면 값을 넣고
# `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/purge`로 부릅니다.
CRON_SECRET=
